{
  "name": "face-value",
  "private": true,
  "description": "Root build tooling: esbuild + typescript used by `sam build` for the TypeScript Lambdas (each Lambda's own deps live in src/*/package.json).",
  "devDependencies": {
    "esbuild": "^0.28.0",
    "typescript": "^5.6.0"
  }
}
